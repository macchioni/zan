---
layout: layouts/base.njk
title: Blog
---

<h1>Blog</h1>

<p class="blog-intro">Pensieri, appunti e riflessioni.</p>

{% if collections.posts.length %}
  <ul class="post-grid">
  {% for post in collections.posts %}
    <li class="post-card">
      <time class="post-date">{{ post.date | readableDate }}</time>
      <h2 class="post-title">
        <a href="{{ post.url }}" class="post-link">{{ post.data.title }}</a>
      </h2>
      {% if post.data.description %}
        <p class="post-excerpt">{{ post.data.description }}</p>
      {% endif %}
    </li>
  {% endfor %}
  </ul>
{% else %}
  <p>Nessun post pubblicato finora.</p>
{% endif %}